# Gemini CLI Bootstrap Notes

- Set `runtime.platform` to `gemini-cli`.
- Set `runtime.adapter_path` to `harness/adapters/gemini-cli`.
- Assume sequential role execution unless the project overrides the adapter defaults.
- Preserve the same artifact and QA expectations even when runtime capabilities are reduced.
