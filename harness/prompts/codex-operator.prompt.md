# Codex Operator Prompt Compatibility Note

The active Codex operator prompt now lives at `harness/adapters/codex/operator.prompt.md`.

Keep this compatibility file only for legacy references. New projects should point `runtime.adapter_path` at `harness/adapters/codex` and load the adapter files from there.
